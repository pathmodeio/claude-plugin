{
  "name": "pathmode-claude-plugin",
  "private": true,
  "version": "0.0.0",
  "description": "Development harness for the Pathmode Claude Code plugin. The plugin's own version lives in .claude-plugin/plugin.json; this file is never published to npm.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "validate": "claude plugin validate . --strict",
    "check:skills": "node scripts/check-skill-drift.mjs"
  },
  "devDependencies": {
    "vitest": "^4.1.8"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
