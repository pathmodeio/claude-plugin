{
    "mcpServers": {
        "pathmode": {
            "command": "npx",
            "args": ["-y", "@pathmode/mcp-server@1.35.2"],
            "env": {
                "PATHMODE_API_KEY": "${user_config.api_key}"
            }
        }
    }
}
