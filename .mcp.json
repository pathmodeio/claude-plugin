{
    "mcpServers": {
        "pathmode": {
            "command": "npx",
            "args": ["-y", "@pathmode/mcp-server@1.26.0"],
            "env": {
                "PATHMODE_API_KEY": "${user_config.api_key}"
            }
        }
    }
}
